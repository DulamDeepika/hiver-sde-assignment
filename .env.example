# Optional LLM API Configuration
# If not provided, the agent automatically runs in high-quality deterministic mode
# with 100% offline support and zero external API dependencies.

OPENAI_API_KEY=your_openai_api_key_here
LLM_PROVIDER=deterministic
